# leetcode560. 和为 K 的子数组：给你一个整数数组 nums 和一个整数 k ，请你统计并返回 该数组中和为 k 的子数组的个数 。子数组是数组中元素的连续非空序列。

# 一句话：如果前缀和 pre_sum - k在哈希表中,说明pre_sum 减去 pre_sum-k能够得到k，就存在连续子数组和为k;有多少个pre_sum - k，就有多少个这样的子数组


---

# （0）先用纯人话讲清楚解题思路（不写代码）

这道题要数的是：

**有多少个“连续的一段”，它们加起来正好等于 `k`。**

比如数组是：

`[1, 2, 1, -1, 2, 1]`

如果 `k = 3`，那么要找的是所有和为 3 的连续片段。

---

## 1. 最直接的想法是什么

最直接的想法是：

* 先固定起点
* 再不断往右扩展终点
* 每扩展一次，就算一下这一段的和
* 如果等于 `k`，答案加 1

这种方法肯定能做出来，但是比较慢。

因为每一个起点都要往后试很多次，整体会做到平方级别的时间复杂度，也就是 **O(n²)**。

---

## 2. 更快的方法的核心想法：不要反复重算一段区间的和

要想快，就要学会“把以前算过的信息存起来”。

这里最关键的工具叫：

## 前缀和

“前缀和”的意思就是：

**从数组开头，一直加到当前位置，得到的总和。**

例如数组：

`[1, 2, 1, -1, 2, 1]`

从左到右累加：

* 还没开始前：0
* 看到 1 后：1
* 看到 2 后：3
* 看到 1 后：4
* 看到 -1 后：3
* 看到 2 后：5
* 看到 1 后：6

所以前缀和序列可以看成：

`0, 1, 3, 4, 3, 5, 6`

---

## 3. 为什么前缀和能解决“子数组求和”

假设：

* 从开头加到位置 `r` 的总和是 `S(r)`
* 从开头加到位置 `l-1` 的总和是 `S(l-1)`

那么从 `l` 到 `r` 这一段的和就是：

`S(r) - S(l-1)`

原因很简单：

* `S(r)` 里面包含了从开头到 `r` 的全部
* `S(l-1)` 里面包含了从开头到 `l-1` 的全部
* 两者一减，前面重复的部分被消掉
* 剩下的就是 `l` 到 `r`

---

## 4. 题目要求的是“这一段等于 k”

也就是说，希望：

`S(r) - S(l-1) = k`

把这个式子移项一下：

`S(l-1) = S(r) - k`

这句话特别重要。

意思是：

**当扫描到当前位置时，只要知道“前面有多少个前缀和等于 当前前缀和 - k”，就知道有多少个子数组以当前位置结尾，并且和等于 k。**

---

## 5. 所以要准备一个“计数表”

这个表用来记录：

**某个前缀和出现过多少次。**

例如：

* 前缀和 0 出现过 1 次
* 前缀和 3 出现过 2 次
* 前缀和 5 出现过 1 次

那么当当前总和是 `6`，目标 `k = 3` 时：

* 需要找的是 `6 - 3 = 3`
* 如果前面前缀和 3 出现过 2 次
* 就说明有 **2 个不同起点**
* 所以就有 **2 个子数组** 以当前位置结尾，且和为 3

---

## 6. 这份答案的思路到底在做什么

这份答案里有三个核心变量：

* `s`：当前前缀和
* `cnt`：计数表，记录“某个前缀和出现了几次”
* `ans`：最后答案

然后从左到右扫数组，每次处理一个数 `x`：

### 第一步：先把当前这个位置之前的前缀和记下来

也就是：

`cnt[s] += 1`

注意这里记录的是“加当前元素之前”的和。

第一次循环时，`s = 0`，这一步就相当于记录了：

**前缀和 0 出现了 1 次**

这很重要，因为它表示：

**从下标 0 开始的子数组也要被统计到。**

---

### 第二步：把当前数字加进总和

也就是：

`s += x`

这样就得到了“以当前元素结尾”的前缀和。

---

### 第三步：查前面有没有某个前缀和等于 `s-k`

也就是：

`ans += cnt[s-k]`

因为：

* 当前前缀和是 `s`
* 如果前面某个前缀和是 `s-k`
* 两者相减就是 `k`
* 那么从那个前缀和后面开始，到当前位置为止，就是一个合法子数组

如果这样的前缀和出现了 3 次，就说明有 3 个合法子数组。

---

## 7. 这道题为什么不能直接用滑动窗口

因为数组里可能有 **负数**。

一旦有负数，窗口右移以后：

* 和可能变大
* 也可能变小

这样就没法像“全是正数”的题那样，用双指针稳定控制区间和。

所以这题最经典、最通用的方法就是：

## 前缀和 + 哈希表（计数表）

---

## 8. 整体流程一句话总结

**边走边累加前缀和，边统计“前面出现过多少个 `当前前缀和 - k`”，出现几次，就新增几个答案。**

---

# （1）给出 Python3 标准答案，每一行右边都有中文注释

```python
from typing import List                           # 导入类型注解 List，表示“整数列表”

class Solution:                                  # 定义题目要求的类 Solution
    def subarraySum(self, nums: List[int], k: int) -> int:  # 定义函数：输入数组 nums 和目标和 k，返回满足条件的子数组个数
        cnt = defaultdict(int)                   # cnt 用来统计“某个前缀和出现了多少次”，默认次数为 0；
                                                 # defaultdict，访问不存在的键时会自动给默认值 0
        ans = 0                                  # ans 记录最终答案
        s = 0                                    # s 表示当前前缀和

        for x in nums:                           # 依次取出 nums 中的每一个元素，当前元素记作 x
            cnt[s] += 1                          # 先记录“当前元素之前”的前缀和 s；第一次循环时相当于记录前缀和 0 出现 1 次
            s += x                               # 把当前元素 x 加到前缀和里，得到“以当前元素结尾”的前缀和
            ans += cnt[s - k]                    # 查找之前有多少个前缀和等于 s-k；每找到一个，就对应一个和为 k 的子数组

        return ans                               # 返回最终答案
```

补一句：

这份写法和更常见的下面这种写法是**完全等价**的：

```python
cnt[0] = 1
for x in nums:
    s += x
    ans += cnt[s - k]
    cnt[s] += 1
```

只是“记录前缀和”的时机写法不同，本质逻辑一样。

---

# （2）对代码进行“一个字母一个字母、一句一句”的解释

下面从头到尾解释。

---

## 第 1 行

```python
from typing import List
```

### 这一行的作用

从 Python 的工具库里，把 `List` 取出来，后面写函数参数类型时要用。

### 一个部分一个部分解释

#### `from`

英文含义是“从……里面”。

#### `typing`

这是 Python 里一个和“类型标注”有关的模块名字。

#### `import`

英文含义是“导入”。

#### `List`

表示“列表类型”。

#### 合起来是什么意思

从 `typing` 这个模块里，把 `List` 这个工具拿进来。

### 为什么这里要写这个

因为后面函数参数要写：

```python
nums: List[int]
```

意思是：

`nums` 是一个“里面装着整数的列表”。

---

## 第 2 行

```python
from collections import defaultdict
```

### 这一行的作用

导入 `defaultdict`。

### 为什么需要它

普通字典在访问一个不存在的键时会报错。

例如普通字典里没有键 `5`，直接写：

```python
d[5]
```

就会出错。

而 `defaultdict(int)` 不一样：

* 如果某个键不存在
* 它会自动给这个键一个默认值 `0`

这题特别适合这个工具，因为“某个前缀和出现多少次”一开始经常还没有记录过，默认就是 0。

---

## 第 4 行

```python
class Solution:
```

### 这一行的作用

定义一个类，类名叫 `Solution`。

LeetCode 这类平台通常要求把答案写在 `Solution` 这个类里面。

### 一个部分一个部分解释

#### `class`

这是 Python 定义“类”的关键字。

#### `Solution`

这是类的名字。

#### `:`

冒号表示：

后面缩进的内容，都属于这个类。

---

## 第 5 行

```python
def subarraySum(self, nums: List[int], k: int) -> int:
```

这一行很长，拆开解释。

---

### `def`

表示“定义一个函数”。

---

### `subarraySum`

这是函数名字。

题目要求实现的函数就叫这个名字。

---

### `(`

左括号，表示参数列表开始。

---

### `self`

在类里面定义函数时，通常第一个参数写 `self`。

初学阶段可先把它理解成：

**这是类里函数的固定写法。**

这题真正需要关心的是后面的 `nums` 和 `k`。

---

### `nums`

这是第一个真正有意义的参数名字。

表示输入数组。

---

### `: List[int]`

这一段表示给 `nums` 加上类型说明：

* `List`：列表
* `[int]`：列表里面每个元素都是整数

也就是：

`nums` 是“整数列表”。

---

### `,`

逗号，表示前一个参数结束，后一个参数开始。

---

### `k`

第二个真正有意义的参数名字。

表示目标和。

---

### `: int`

表示 `k` 是一个整数。

---

### `)`

右括号，表示参数列表结束。

---

### `-> int`

表示这个函数最终返回的是一个整数。

这题返回的是“子数组个数”，当然是整数。

---

### `:`

冒号表示：

下面缩进的代码都属于这个函数。

---

## 第 6 行

```python
cnt = defaultdict(int)
```

这一行非常重要。

---

### 这一行的作用

创建一个“计数表”，名字叫 `cnt`。

它的功能是：

**记录某个前缀和出现了多少次。**

---

### 一个字母一个字母解释 `cnt`

#### `c`

是变量名的一部分。

#### `n`

也是变量名的一部分。

#### `t`

也是变量名的一部分。

合起来 `cnt` 是 `count` 的简写，意思是“计数”。

---

### `=`

等号在程序里不是数学里的“左右相等”的意思。

这里表示：

**把右边创建出来的东西，交给左边这个变量保存。**

---

### `defaultdict(int)`

这表示创建一个默认值为整数 0 的字典。

为什么是 0？

因为这里存的是“出现次数”。

没出现过，次数自然就是 0。

---

### 举个直观例子

假设：

```python
cnt = defaultdict(int)
```

此时如果写：

```python
cnt[100]
```

不会报错，而是得到 `0`。

因为 `100` 这个前缀和还没出现过，所以默认次数是 0。

---

## 第 7 行

```python
ans = 0
```

### 这一行的作用

定义答案变量 `ans`，初始值是 0。

---

### 一个字母一个字母解释 `ans`

#### `a`

变量名的一部分。

#### `n`

变量名的一部分。

#### `s`

变量名的一部分。

合起来 `ans` 是 `answer` 的简写，意思是“答案”。

---

### 为什么初始值是 0

因为一开始还没有找到任何满足条件的子数组，所以答案当然从 0 开始。

---

## 第 8 行

```python
s = 0
```

### 这一行的作用

定义前缀和变量 `s`，初始值为 0。

---

### 一个字母一个字母解释 `s`

#### `s`

这里通常理解成 `sum` 的简写，意思是“和”。

---

### 为什么一开始是 0

因为在还没看任何数组元素之前，累加和就是 0。

这个“开始前的 0”很重要。

它代表：

**从数组开头开始的那种子数组，也必须能被统计到。**

---

## 第 10 行

```python
for x in nums:
```

这一行表示开始循环。

---

### `for`

表示“对……中的每一个，依次处理”。

---

### `x`

这是循环里当前拿到的一个元素。

名字随便取，写成 `x`、`num`、`value` 都行。

这里只是用了最短的写法 `x`。

---

### `in`

表示“在……里面”。

---

### `nums`

表示在数组 `nums` 里面，一个一个地取元素。

---

### `:`

冒号表示：

下面缩进的代码，每一次循环都要执行一次。

---

### 整行翻成人话

把数组 `nums` 里的元素，从左到右一个一个取出来，每次取到的当前元素叫 `x`。

---

## 第 11 行

```python
cnt[s] += 1
```

这一行是整道题里最容易让初学者疑惑的地方。

必须仔细讲。

---

### 先解释整体作用

这一步是在说：

**先把“当前元素之前的前缀和”记录下来。**

注意，是“当前元素之前”。

不是当前元素加进去之后。

---

### 一个部分一个部分解释

#### `cnt[s]`

表示在字典 `cnt` 里，用 `s` 作为键，找到对应的次数。

意思是：

“前缀和等于 `s` 的情况，出现过多少次”。

---

#### `+= 1`

这不是两个操作，而是一个整体运算符。

意思是：

**在原来的基础上，加 1。**

例如：

```python
a += 1
```

等价于：

```python
a = a + 1
```

---

### 所以 `cnt[s] += 1` 的完整意思是

把“前缀和 `s` 出现的次数”加 1。

---

### 为什么这里要先记录 `s`

因为当前这一轮还没把 `x` 加进去。

所以此时的 `s` 表示的是：

**当前元素左边那一段的总和。**

而这正是后面构造子数组起点时需要的“前面的前缀和”。

---

### 第一次循环为什么尤其重要

一开始 `s = 0`。

第一次进入循环时先执行：

```python
cnt[0] += 1
```

这样就记录了：

**前缀和 0 出现过 1 次。**

这个“0”代表的是：

**在数组开始之前的位置。**

这样一来，如果从下标 0 开始到某个位置的和正好等于 `k`，就能被正确统计。

---

### 这一步可以理解成什么

可以理解为：

在处理当前元素之前，先把“到当前元素左边为止”的累计和，存入计数表，供后面查找使用。

---

## 第 12 行

```python
s += x
```

### 这一行的作用

把当前元素 `x` 加进前缀和 `s` 里。

---

### 一个部分一个部分解释

#### `s`

当前累计和。

#### `+=`

在原来的基础上加上右边的值。

#### `x`

当前取到的数组元素。

---

### 这一行执行完以后，`s` 变成什么

变成：

**从数组开头一直加到当前元素为止的总和。**

也就是说：

当前这一轮处理完成后，`s` 表示的是“以当前元素结尾”的前缀和。

---

## 第 13 行

```python
ans += cnt[s - k]
```

这一行是最核心的一行。

---

### 这一行的整体作用

把“以前出现过多少个 `s-k`”加到答案里。

因为每出现一个这样的前缀和，就对应一个和为 `k` 的子数组。

---

### 一个部分一个部分解释

#### `ans`

总答案。

---

#### `+=`

在原有答案基础上继续增加。

---

#### `cnt[...]`

去计数表中查某个前缀和出现过多少次。

---

#### `s - k`

这是整个题目的灵魂。

当前前缀和是 `s`。

想找某个更早的前缀和 `p`，使得：

`当前这一段的和 = k`

也就是：

`p` 到当前 `s` 之间差了 `k`

于是：

`p = s - k`

所以只要查一查：

**前面有多少个前缀和等于 `s-k`**

就能知道当前新增多少个合法子数组。

---

### 为什么“有几个这样的前缀和”，就“新增几个答案”

因为每一个不同的位置，都会对应一个不同的子数组起点。

前缀和相同，可能出现很多次，表示有很多个不同位置都能作为起点。

所以：

* `cnt[s-k] = 0`，说明当前没有新增合法子数组
* `cnt[s-k] = 1`，说明新增 1 个
* `cnt[s-k] = 2`，说明新增 2 个
* 以此类推

---

### 举个非常小的例子

假设当前 `s = 6`，`k = 3`。

那么需要找：

`6 - 3 = 3`

如果前面前缀和 3 出现了 2 次，那么就说明：

* 有两个不同的位置，前缀和都等于 3
* 从这两个位置后面开始，到当前位置结束
* 都能组成和为 3 的子数组

所以答案要加 2。

---

## 第 15 行

```python
return ans
```

### 这一行的作用

把最终答案返回出去。

---

### `return`

表示“把结果交出去”。

---

### `ans`

前面累积得到的总个数。

---

## 整个循环每次到底在干什么

每一轮都做 3 件事：

### 第 1 件

先记录当前元素之前的前缀和

```python
cnt[s] += 1
```

---

### 第 2 件

把当前元素加进来，得到当前位置的前缀和

```python
s += x
```

---

### 第 3 件

看前面有多少个前缀和等于 `s-k`

```python
ans += cnt[s-k]
```

---

## 为什么这段代码顺序不能乱

这题这份代码采用的是这种风格：

* 先记录“旧前缀和”
* 再加当前数
* 再查答案

这种写法的逻辑非常完整。

如果顺序乱掉，就容易把“当前元素之前的前缀和”和“当前元素之后的前缀和”混在一起。

---

## 这一版代码与常见写法的对应关系

这份代码：

```python
for x in nums:
    cnt[s] += 1
    s += x
    ans += cnt[s - k]
```

和更常见的：

```python
cnt[0] = 1
for x in nums:
    s += x
    ans += cnt[s - k]
    cnt[s] += 1
```

本质一样。

区别只在于：

* 第一种：每轮开头记录“旧的前缀和”
* 第二种：开头先手动记录 `0`，每轮结尾记录“新的前缀和”

只是写法不同，数学含义相同。

---

# （3）给一个具体数值算例，并用表格展示每一步关键变量变化

下面用这个例子：

```python
nums = [1, 2, 1, -1, 2, 1]
k = 3
```

---

## 先直接把答案列出来，方便核对

所有和为 3 的连续子数组有 5 个：

1. `[1, 2]`
2. `[1, 2, 1, -1]`
3. `[2, 1]`
4. `[1, -1, 2, 1]`
5. `[2, 1]`

所以最终答案应该是：

```python
5
```

---

## 先列出前缀和

| 位置说明          | 前缀和 |
| ------------- | --: |
| 还没开始（相当于下标左边） |   0 |
| 看到第 1 个数 1 后  |   1 |
| 看到第 2 个数 2 后  |   3 |
| 看到第 3 个数 1 后  |   4 |
| 看到第 4 个数 -1 后 |   3 |
| 看到第 5 个数 2 后  |   5 |
| 看到第 6 个数 1 后  |   6 |

---

## 按代码执行顺序，逐轮模拟

说明：

* `s(进入本轮前)`：执行这一轮之前的前缀和
* `cnt 更新后`：执行 `cnt[s] += 1` 之后的有效计数表
* `s(加上 x 后)`：执行 `s += x` 之后的新前缀和
* `需要查找`：`s-k`
* `找到次数`：`cnt[s-k]`
* `新增子数组`：这一轮新找到的合法子数组
* `ans`：当前累计答案

下面表格里只展示“次数大于 0”的键，便于阅读。

| 轮次 | 当前元素 `x` | `s`（进入本轮前） | 执行 `cnt[s] += 1` 后的 `cnt`        | `s += x` 后的新 `s` | 需要查找 `s-k` | 找到次数 `cnt[s-k]` | 本轮新增的合法子数组               | `ans` |
| -: | -------: | ---------: | -------------------------------- | ---------------: | ---------: | --------------: | ------------------------ | ----: |
|  1 |        1 |          0 | `{0: 1}`                         |                1 |         -2 |               0 | 无                        |     0 |
|  2 |        2 |          1 | `{0: 1, 1: 1}`                   |                3 |          0 |               1 | `[1, 2]`                 |     1 |
|  3 |        1 |          3 | `{0: 1, 1: 1, 3: 1}`             |                4 |          1 |               1 | `[2, 1]`                 |     2 |
|  4 |       -1 |          4 | `{0: 1, 1: 1, 3: 1, 4: 1}`       |                3 |          0 |               1 | `[1, 2, 1, -1]`          |     3 |
|  5 |        2 |          3 | `{0: 1, 1: 1, 3: 2, 4: 1}`       |                5 |          2 |               0 | 无                        |     3 |
|  6 |        1 |          5 | `{0: 1, 1: 1, 3: 2, 4: 1, 5: 1}` |                6 |          3 |               2 | `[1, -1, 2, 1]`、`[2, 1]` |     5 |

---

# （4）凡是出现公式或关键表达式，都解释来源

下面把本题最关键的公式全部解释清楚。

---

## 公式 1：前缀和的定义

设 `pre[i]` 表示从数组开头到下标 `i` 的元素和，那么：

`pre[i] = nums[0] + nums[1] + ... + nums[i]`

### 这个公式的来源

这是“前缀和”这个概念本身的定义。

“前缀”就是从最前面开始的一段。

所以“前缀和”自然就是从第一个数开始，一直加到当前位置。

---

## 公式 2：子数组和 = 两个前缀和之差

从下标 `l` 到下标 `r` 的子数组和为：

`pre[r] - pre[l-1]`

### 这个公式的来源

因为：

* `pre[r]` 包含了 `nums[0]` 到 `nums[r]`
* `pre[l-1]` 包含了 `nums[0]` 到 `nums[l-1]`

两者相减时，前面那一大段重复内容被抵消，只剩下：

`nums[l] + nums[l+1] + ... + nums[r]`

这正好就是子数组 `l 到 r` 的和。

---

## 公式 3：题目要求子数组和等于 k

希望：

`pre[r] - pre[l-1] = k`

### 这个式子的来源

因为题目要求“连续子数组的和为 k”。

而子数组和已经由公式 2 表达成了前缀和之差，所以直接写成这个式子。

---

## 公式 4：移项得到 `pre[l-1] = pre[r] - k`

从

`pre[r] - pre[l-1] = k`

移项可得：

`pre[l-1] = pre[r] - k`

### 这个式子的来源

纯粹是代数变形。

这个变形的意义非常大，因为它把“找一个子数组”变成了：

**找前面是否出现过某个特定的前缀和。**

---

## 公式 5：为什么代码里查的是 `cnt[s-k]`

代码中的 `s` 就相当于“当前的前缀和”，也就是 `pre[r]`。

根据上面的推导：

`pre[l-1] = pre[r] - k`

代入代码变量名，就是：

`前面的前缀和 = s - k`

所以只要查：

```python
cnt[s-k]
```

就知道前面有多少个位置可以作为合法起点。

---

## 公式 6：为什么 `cnt[s-k]` 可以直接加到答案里

因为 `cnt[s-k]` 表示：

**前缀和等于 `s-k` 的位置，一共出现了多少次。**

每出现一次，就对应一个不同的子数组起点。

所以当前这一轮新增的合法子数组个数，正好就是：

`cnt[s-k]`

因此代码写成：

```python
ans += cnt[s-k]
```

---

## 公式 7：为什么第一次必须考虑前缀和 0

当子数组从下标 `0` 开始时，按照公式：

`pre[r] - pre[l-1]`

这里 `l = 0`，所以 `l - 1 = -1`。

于是需要定义：

`pre[-1] = 0`

这表示“在数组开始之前，前缀和是 0”。

### 这个定义的作用

这样从开头开始的子数组也能统一纳入公式处理，不需要单独写特殊情况。

---

## 为什么这份代码里没有直接写 `cnt[0] = 1`

因为它用了另一种等价写法：

在每轮开始先执行：

```python
cnt[s] += 1
```

第一次循环开始时，`s` 本来就是 0，所以相当于自动完成了：

```python
cnt[0] = 1
```

所以虽然没有手写这一行，但效果是一样的。

---

