下面给出一个**更复杂、一步一步“杀死”滑动窗口的例子**。
不是简单的小数组，而是**看起来很“正常”、很容易让人误判可以用滑动窗口**的那种。

---

# 一、构造一个“看起来很合理、但必死”的例子

给定你提到的数字规模，构造如下数组（只做**最小修改**）：

```
nums = [12, 15, -17, 20, -10, 5, 7]
k = 25
```

这个数组的特点：

* 大数为主（12、15、20）
* 负数夹在中间（-17、-10）
* **非常像“可以滑动窗口”的那种数据**
* 但实际上：滑动窗口必错

---

# 二、先给出「真实正确答案」（作为对照）

下面列出**所有和为 25 的连续子数组**：

| 子数组                       | 计算               | 和  |
| ------------------------- | ---------------- | -- |
| [12, 15, -17, 20, -10, 5] | 12+15-17+20-10+5 | 25 |
| [20, -10, 5, 7, 3] ❌      | 不存在              | ❌  |
| [15, -17, 20, 7]          | 15-17+20+7       | 25 |
| [20, 5]                   | 20+5             | 25 |

✅ **正确答案数量 = 3**

---

# 三、滑动窗口一步一步执行（完整崩溃演示）

## 初始化

```
left = 0
sum = 0
```

---

## Step 1：right = 0，加入 12

```
sum = 12   (<25)
→ 扩大窗口
```

---

## Step 2：right = 1，加入 15

```
sum = 27   (>25)
→ 按滑动窗口逻辑：左指针右移
```

移除 12：

```
sum = 15
left = 1
```

⚠️ **第一次错误决策已经发生**

因为：

* 正确解 `[12, 15, -17, 20, -10, 5]`
* 被提前“砍掉了 12”
* 后面无论怎么滑，都回不来了

---

## Step 3：right = 2，加入 -17

```
sum = -2   (<25)
→ 扩大窗口
```

---

## Step 4：right = 3，加入 20

```
sum = 18   (<25)
→ 扩大窗口
```

---

## Step 5：right = 4，加入 -10

```
sum = 8   (<25)
→ 扩大窗口
```

---

## Step 6：right = 5，加入 5

```
sum = 13   (<25)
→ 扩大窗口
```

---

## Step 7：right = 6，加入 7

```
sum = 20   (<25)
→ 结束
```

---

## 滑动窗口最终结果

```
找到 0 个
```

❌ **真实答案是 3 个**

---

# 四、滑动窗口在这个例子中犯了哪些“原则性错误”

## 错误 1：误以为「sum > k → 一定要缩窗口」

在 Step 2：

```
12 + 15 = 27 > 25
```

滑动窗口假设：

> 去掉左边元素，和会变小，答案更接近

但真实情况是：

```
后面马上会出现 -17
```

这会让：

```
27 - 17 = 10
```

**窗口如果不缩，反而是对的**

---

## 错误 2：滑动窗口做了“不可逆决策”

一旦执行：

```
left += 1
```

意味着：

> 所有以 nums[0] 开头的子数组
> 永久不再考虑

而真正的答案：

```
[12, 15, -17, 20, -10, 5]
```

正是从 `nums[0]` 开始的

---

# 五、同一例子，用前缀和 + 哈希表一定成功

## 前缀和表（完整）

| i  | nums[i] | prefix_sum | prefix_sum - k |
| -- | ------- | ---------- | -------------- |
| -1 | —       | 0          | -25            |
| 0  | 12      | 12         | -13            |
| 1  | 15      | 27         | 2              |
| 2  | -17     | 10         | -15            |
| 3  | 20      | 30         | 5              |
| 4  | -10     | 20         | -5             |
| 5  | 5       | 25         | 0 ✅            |
| 6  | 7       | 32         | 7              |

### 关键点

* `prefix_sum = 25`
* 查 `25 - 25 = 0`
* **前缀和 0 在最开始就存在**

→ 找到第一个答案

---

## 为什么前缀和一定不会漏？

因为它遵循的是：

```
只要存在两个位置 i < j
使得 prefix[j] - prefix[i] = k
```

而不是：

```
当前窗口大了还是小了？
```

**完全不依赖趋势、方向、正负号**

---

# 六、终极结论（必须能背出来）

> **滑动窗口不是“通用算法”**
> 它成立的前提只有一个：
>
> 👉 **窗口扩大时，和必须单调不减**

一旦数组中存在：

* 负数
* 或者无法保证单调性

👇

> **滑动窗口必然在“提前缩窗口”时，把正确答案永久删掉**
